export const Tag = ({ label }: { label: string }) => (
  <span className="inline-block px-2 py-[3px] bg-bg-elevated border border-border rounded text-[11px] text-text-muted tracking-[0.04em]">
    {label}
  </span>
)
